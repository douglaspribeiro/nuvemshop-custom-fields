spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false

nuvemshop:
  client-id: test-client
  client-secret: test-secret
  redirect-uri: http://localhost/oauth/callback
  auth-url: https://example.com/auth
  token-url: https://example.com/token
  api-base-url: https://api.example.com
  app-base-url: http://localhost:8080
  scopes: read_products,read_orders,write_scripts,read_scripts,billing,read_store
  user-agent: NuvemCustomFields tests
  billing:
    enabled: false
    api-base-url: https://api.example.com/2025-03
    concept-code:

shopify:
  client-id: shopify-client
  client-secret: shopify-secret
  scopes: read_products
  app-base-url: http://localhost:8080
  api-version: 2026-04

backoffice:
  username: admin
  password: admin
